# Web app config. Copy to .env for local dev.

# When "true", the app talks to the Tadka backend (server/) instead of using
# the built-in mock data. Leave unset/false to run the app standalone.
VITE_USE_BACKEND=false

# Base URL of the Tadka backend.
VITE_API_BASE=http://localhost:8787
